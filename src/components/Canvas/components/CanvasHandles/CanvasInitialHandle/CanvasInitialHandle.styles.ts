import styled from 'styled-components'

export const CanvasInitialHandleElement = styled.div`
  & > .react-flow__handle-right {
    right: -45px;
    background-color: transparent;
    border-color: transparent;
  }
`
